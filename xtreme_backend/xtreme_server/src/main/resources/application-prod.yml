spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/xtreme_shop?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: ${SERVER_PORT:8080}
