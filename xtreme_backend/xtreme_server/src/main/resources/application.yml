spring:
  application:
    name: xtreme_server
  profiles:
    active: dev

server:
  port: 8080

knife4j:
  enable: true

# spring-dotenv 配置：从项目根目录加载 .env 文件
# IntelliJ 运行时工作目录默认为 xtreme_server 模块目录，.env 在上两级
dotenv:
  directory: "../../"
